require 'db.php';
$region_id = $_GET['region_id'];
$result = $conn->query("SELECT id, name FROM provinces WHERE region_id = $region_id");
while ($row = $result->fetch_assoc()) {
    echo "<option value='{$row['id']}'>{$row['name']}</option>";
}
